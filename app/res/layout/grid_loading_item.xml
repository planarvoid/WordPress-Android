<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/track_info_row"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

    <RelativeLayout android:layout_width="match_parent"
                    android:layout_height="@dimen/gridview_loading_height"
                    tools:ignore="UselessParent">

        <ProgressBar android:id="@id/ak_list_progress"
                     android:indeterminateOnly="true"
                     android:layout_width="wrap_content"
                     android:layout_height="wrap_content"
                     android:layout_centerInParent="true"
                     android:gravity="center"/>

        <LinearLayout android:id="@id/ak_list_retry"
                      android:layout_width="match_parent"
                      android:layout_height="@dimen/gridview_loading_height"
                      android:gravity="center"
                      android:visibility="gone"
                      android:orientation="vertical">

            <View style="@style/HorizontalDivider"/>

            <TextView android:layout_width="wrap_content"
                      android:layout_height="0dp"
                      android:layout_weight="1"
                      style="@style/Ak.EmptyViewContent"
                      android:gravity="center"
                      android:text="@string/ak_error_pager_failed" />
        </LinearLayout>
    </RelativeLayout>
</RelativeLayout>