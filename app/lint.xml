<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <issue id="MissingTranslation">
        <ignore path="res/values/dev.xml" />
        <ignore path="res/values/keys.xml" />
        <ignore path="res/values/array.xml" />
        <ignore path="res/values/sigs.xml" />
        <ignore path="res/values/dimens.xml" />
        <ignore path="res/values/array.xml" />
        <ignore path="res/values/colors.xml" />
    </issue>

    <issue id="ViewConstructor" severity="ignore"/>
    <issue id="TypographyEllipsis" severity="ignore"/>

    <!-- change before release -->
    <issue id="MissingTranslation" severity="ignore"/>

    <!-- should probably re-enable later once more important things are fixed -->
    <issue id="ContentDescription" severity="ignore"/>


    <!-- re-enable before release   -->
    <issue id="NewApi" severity="error"/>

    <issue id="NewApi">
        <!-- make sure these still work on 2.2 -->
        <ignore path="res/layout/abs__search_view.xml"/>
        <ignore path="res/layout/dashboard_notification_v11.xml"/>
        <ignore path="res/layout/playback_status_v11.xml"/>
        <ignore path="res/layout/upload_status.xml"/>
    </issue>

    <!-- can't resolve mvn dependencies at this point -->
    <issue id="MissingRegistered" severity="ignore"/>
</lint>
