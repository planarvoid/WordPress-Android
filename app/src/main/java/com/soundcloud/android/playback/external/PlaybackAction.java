package com.soundcloud.android.playback.external;

import com.soundcloud.android.BuildConfig;

public class PlaybackAction {

    public static final String TOGGLE_PLAYBACK = BuildConfig.APPLICATION_ID + ".playback.external.toggleplayback";
    public static final String PLAY = BuildConfig.APPLICATION_ID + ".playback.external.play";
    public static final String PAUSE = BuildConfig.APPLICATION_ID + ".playback.external.pause";
    public static final String PREVIOUS = BuildConfig.APPLICATION_ID + ".playback.external.previous";
    public static final String NEXT = BuildConfig.APPLICATION_ID + ".playback.external.next";
    public static final String CLOSE = BuildConfig.APPLICATION_ID + ".playback.external.close";

}
